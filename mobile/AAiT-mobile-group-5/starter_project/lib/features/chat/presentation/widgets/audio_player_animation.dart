import 'package:flutter/material.dart';

class AudioPlayerAnimation extends StatelessWidget {
  final String text;
  final bool isSender;
  final bool isLast;

  const AudioPlayerAnimation(
      {super.key,
      required this.text,
      required this.isSender,
      required this.isLast});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
